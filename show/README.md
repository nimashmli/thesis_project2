# 📊 EEG Emotion Recognition - Results Viewer

وبسایت PHP برای نمایش و مقایسه نتایج آزمایش‌های EEG Emotion Recognition

## 🚀 نحوه اجرا

### روش 1: استفاده از PHP Built-in Server

```bash
cd show
php -S localhost:8000
```

سپس در مرورگر به آدرس `http://localhost:8000` بروید.

### روش 2: استفاده از XAMPP/WAMP

1. فایل‌های را در پوشه `htdocs` یا `www` کپی کنید
2. در مرورگر به آدرس `http://localhost/show` بروید

### روش 3: استفاده از Apache/Nginx

فایل‌ها را در پوشه web server قرار دهید و از طریق مرورگر دسترسی پیدا کنید.

## 📁 ساختار فایل‌ها

```
show/
├── index.php      # فایل اصلی وبسایت
├── api.php        # API برای بارگذاری داده‌ها
└── README.md      # این فایل
```

## ✨ ویژگی‌ها

### 🎨 رابط کاربری زیبا
- طراحی مدرن و responsive
- رنگ‌بندی جذاب با gradient
- انیمیشن‌های نرم

### 📈 قابلیت‌های نمودار
- **دو نمودار جداگانه**: یکی برای Accuracy و یکی برای Loss
- **افزودن چند نمودار**: با دکمه + می‌توانید چندین منحنی اضافه کنید
- **انتخاب دقیق**: انتخاب Model، Validation Type، Run، Subject و Fold
- **نام‌گذاری**: می‌توانید برای هر منحنی نام دلخواه بگذارید
- **انتخاب رنگ**: هر منحنی می‌تواند رنگ مخصوص خودش را داشته باشد
- **تغییر عنوان**: عنوان نمودارها قابل تغییر است
- **حذف منحنی**: می‌توانید هر منحنی را جداگانه حذف کنید

### 🔍 انتخاب داده
- انتخاب نوع داده: Train یا Test
- انتخاب نوع نمودار: Accuracy یا Loss
- انتخاب معماری: simpleNN, cnn_45138, capsnet2020, hippoLegS1
- انتخاب Validation Type: subject_dependent یا subject_independent
- انتخاب Run: run1, run2, ...
- انتخاب Subject: برای subject_dependent
- انتخاب Fold: 0, 1, 2, 3, 4

## 📋 نحوه استفاده

1. **افزودن نمودار**: روی دکمه "افزودن نمودار" کلیک کنید
2. **انتخاب گزینه‌ها**: 
   - نوع نمودار (Accuracy یا Loss)
   - نوع داده (Train یا Test)
   - Model, Validation, Run, Subject (در صورت نیاز), Fold
   - نام منحنی و رنگ
3. **افزودن**: روی "افزودن به نمودار" کلیک کنید
4. **مقایسه**: می‌توانید چندین منحنی را همزمان اضافه کنید و مقایسه کنید

## 🎯 مثال استفاده

### مقایسه Train و Test Accuracy برای یک Subject:
1. افزودن نمودار → Accuracy → Train → انتخاب Model/Run/Subject/Fold → نام: "Train Acc" → افزودن
2. افزودن نمودار → Accuracy → Test → همان انتخاب‌ها → نام: "Test Acc" → افزودن

### مقایسه چند Subject:
1. افزودن نمودار → Accuracy → Test → Subject 0 → نام: "Subject 0"
2. افزودن نمودار → Accuracy → Test → Subject 1 → نام: "Subject 1"
3. و غیره...

## ⚙️ تنظیمات

اگر پوشه `result` در مسیر پیش‌فرض نیست، می‌توانید مسیر را در فایل `api.php` در تابع `findResultsPath()` تغییر دهید.

## 📝 نیازمندی‌ها

- PHP 7.4 یا بالاتر
- دسترسی خواندن به پوشه `result`
- مرورگر مدرن (Chrome, Firefox, Edge)

## 🎨 کتابخانه‌های استفاده شده

- **Chart.js**: برای رسم نمودارها
- **Vanilla JavaScript**: برای منطق برنامه
- **PHP**: برای API و بارگذاری داده‌ها

## 💡 نکات

- نمودارها به صورت real-time به‌روزرسانی می‌شوند
- می‌توانید نام و رنگ منحنی‌ها را بعد از افزودن تغییر دهید
- عنوان نمودارها قابل ویرایش است
- برای حذف یک منحنی، روی دکمه "حذف" کنار آن کلیک کنید


